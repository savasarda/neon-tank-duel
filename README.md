# Neon Tank Duel

Mobile-first, online two-player tank duel with bouncing bullets and Socket.IO multiplayer.

## Run locally

```bash
npm install
npm run dev
```

Open `http://localhost:5173` in two browsers or phones on the same network.
